version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies

  pre_build:
    commands:
      - echo Pre-build started

  build:
    commands:
      - echo Building Docker image
      - docker build -t cloudtech346/devbank-frontend-v2:latest .

  post_build:
    commands:
      - echo Build completed

artifacts:
  files:
    - '**/*'
